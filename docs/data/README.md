# 法規資料結構

每個法規模組各自一個 JSON 檔案，並由 `manifest.json` 統一註冊。

- `rules`: 完整規則資料，包含歷史沿革與未來已公告規定，透過 `status` 區分
- `current`: 只有在「現行整併後的適用範圍」與 `rules` 不同時才需要提供；未提供時，前端會自動以 `rules` 中非「未來實施」規則作為現行資料
- `areas`: 陣列格式，可讓同一規則同時歸類到「餐飲」與「製造」等多個業態

新增法規模組時：新增 JSON 檔 → 在 `manifest.json` 加一筆即可，前端不需要修改核心程式。
