# Food Regulation Dashboard Maintenance Guide

本檔案是 `food-reg-dashboard` 的維護規範。任何新增法規、修正既有法規、更新法規版本、調整分類或修改前端查詢邏輯前，都應先閱讀本檔案。

## 1. Repository 與發布流程

- Repository：`ypy-cloud/food-reg-dashboard`
- `main`：正式發布版本
- `dev`：修改、驗證版本
- GitHub Pages：`main /docs`
- 正式網站：`https://ypy-cloud.github.io/food-reg-dashboard/`

正常更新流程：

1. 先讀取 `AGENTS.md`
2. 讀取 `README.md`
3. 讀取 `docs/data/manifest.json`
4. 讀取本次涉及的法規 JSON
5. 在 `dev` 修改
6. 驗證資料、查詢、現行／歷史／未來模式
7. 確認無誤後再同步至 `main`
8. 不直接在未確認狀態下修改 `main`

## 2. 資料架構

每一個法規模組使用獨立 JSON，不把法規資料重新塞回 `index.html` 或 JavaScript 常數中。

目前資料位於：

`docs/data/`

主要模組：

- `monitoring-plan.json`：食品安全監測計畫
- `mandatory-testing.json`：強制檢驗
- `haccp.json`：食品安全管制系統 HACCP
- `laboratory.json`：應自設實驗室
- `hygiene-manager.json`：應置衛生管理人員
- `professional-personnel.json`：應置專門職業人員
- `technical-certification.json`：應置技術證照人員
- `liability-insurance.json`：產品責任險
- `traceability.json`：食品追溯追蹤
- `sources.json`：集中管理官方資料來源
- `manifest.json`：法規模組登錄清單

新增法規模組時：

1. 建立新的獨立 JSON
2. 在 `manifest.json` 登錄模組名稱與檔案
3. 必要時在 `sources.json` 新增官方來源
4. 不修改既有模組資料，除非該修法確實影響既有規範

## 3. 法規資料核心原則

### 3.1 不能只看最近一次修正

最新修正公告通常只寫新增或變更的對象，不能因此把原本已經實施的業別刪掉。

更新法規時必須：

- 先讀現有 JSON
- 再讀最新正式公告
- 比對「新增、刪除、門檻變更、日期變更、名稱變更」
- 保留未被修正公告廢止或取代的既有規範
- 不能用「最近一次公告內容」直接覆蓋完整現行規定

### 3.2 草案與正式法規分開

- 預告、草案、修正草案不得直接列入「現行規定」
- 已公告但尚未到實施日期者列入「未來實施」
- 已正式生效者才列入「現行規定」
- 如需保留草案資訊，應另外標示為草案，不得與正式規定混在一起

### 3.3 現行規定應呈現今天真正的適用門檻

若某規定過去曾分階段實施，但目前已全面完成納管：

- `current` 應顯示現在真正適用的完整門檻
- 不要在現行規定中寫「現已不分資本額」、「原分三階段」等歷史轉折語氣
- 歷史階段留在 `rules`／歷史沿革資料

例如：

餐盒食品工廠 HACCP 現行適用條件應直接顯示「須辦理工廠登記之餐盒食品工廠」，歷史每日供應量分階段門檻只留在歷史沿革。

### 3.4 明確數字要直接寫

法規已有明確門檻時，不寫「依公告規模」、「依現行規定」等模糊文字。

應直接列出：

- 資本額
- 食品從業人員人數
- 門市家數
- 輸入量
- 每日供應量
- 技術證照人員比例
- 檢驗頻率
- 實施日期

只有法規真的需要個案判斷時，才使用文字條件。

## 4. 查詢分類規則

### 4.1 業態使用 `areas` 陣列

不要用單一文字 `餐飲／製造` 當作唯一分類。

正確格式例如：

```json
"areas": ["餐飲", "製造"]
```

這樣使用者篩選「餐飲」或「製造」都必須搜尋得到。

主要業態分類目前統一為：

- 製造
- 餐飲
- 輸入
- 販售
- 零售（如資料上確有需要可保留）
- 上市／上櫃（如為特殊法規分類可保留）

「製造／加工／調配」、「製造／加工」原則上都歸為「製造」。

### 4.2 前店後廠小型烘焙業

在「應置技術證照人員」模組中歸類為「餐飲」。

## 5. 現行、歷史、未來資料

每個模組原則上保留：

- `rules`：完整規則集合，包含歷史階段與未來已公告規定
- `current`：截至最後校正日已生效的現行整併結果

網站查詢模式：

- 現行規定：使用 `current`
- 歷史沿革：查看已實施的歷史階段
- 未來實施：只顯示已正式公告但尚未到實施日的規則

例如食品安全監測計畫：

- 2026/1/1 已生效的 2,000 萬～未滿 3,000 萬且 ≥5 人門檻應出現在現行規定
- 2027/1/1、2028/1/1 尚未到期的門檻應只出現在未來實施

## 6. 資料欄位原則

規則至少應包含：

```json
{
  "id": 10001,
  "areas": ["製造"],
  "category": "業別或產品類別",
  "scale": "明確規模／適用條件",
  "start": "YYYY-MM-DD 或既有納管",
  "status": "已實施 或 未來實施",
  "basis": "法源",
  "note": "必要的判讀備註",
  "sourceName": "官方資料名稱",
  "sourceUrl": "官方來源網址"
}
```

### ID

- 既有規則 ID 不任意更改
- 新規則使用新的唯一 ID
- 不得因重新排序而改變 ID

### 日期

- 有明確日期時使用 `YYYY-MM-DD`
- 能查到正式實施日期時，不用模糊的「現行」、「目前」取代
- 若資料來源只能確認既有納管而無法安全確認首次日期，可使用「既有納管」，但應在後續研究時優先補齊

## 7. 官方來源與法規查證

法規更新優先使用官方資料：

1. 衛生福利部食品藥物管理署（TFDA）正式公告
2. 全國法規資料庫／主管機關正式法規全文
3. TFDA 正式 Q&A、附表、公告附件

不得只根據：

- 新聞轉述
- 部落格
- 顧問文章
- 搜尋結果摘要

若官方公告與 Q&A 有差異，以法律效力較高且最新的正式法規／公告為優先，並在備註說明。

## 8. 修法更新標準流程

使用者說「某法規有修法」時：

1. 查最新正式公告
2. 讀現有模組 JSON
3. 建立差異清單：
   - 新增業別
   - 刪除業別
   - 資本額變更
   - 人數變更
   - 產品範圍變更
   - 日期變更
   - 法規名稱變更
4. 判斷是否影響 `current`
5. 若是未來生效，只更新 `rules` 的未來規定，不提前改變 `current`
6. 若已生效，同時更新 `current`
7. 保留必要歷史規則供「歷史沿革」查詢
8. 更新 `manifest.json` 的 `lastReviewed`／版本日期（如適用）
9. 在 `dev` 驗證
10. 再同步 `main`

## 9. 前端維護原則

- 法規內容只放 JSON
- `index.html` 負責頁面結構
- `css/style.css` 負責樣式
- `js/app.js` 負責資料載入、查詢、篩選、CSV、面板互動
- 不重新引入 ZIP、Base64、`part00.txt` 等舊式載入方式
- GitHub Pages 應直接讀取 `docs/data/*.json`

## 10. UI 現有要求

- 首頁不顯示統計方格
- 「已納入法規」有獨立面板
- 「資料來源」有獨立面板
- 不顯示「長官摘要」、「管理階層版」、「給長官看」等字眼
- 不提供列印／存成 PDF 按鈕
- 保留 CSV 匯出
- 首頁次標題只顯示最後校正日期

## 11. 更新完成前檢查

每次提交前至少確認：

- JSON 可正常解析
- `manifest.json` 中的每個檔案都存在
- 每個模組名稱與 JSON 內 `name` 一致
- ID 無重複
- `current` 不包含尚未生效規則
- 未來實施規則沒有混入現行規定
- 原已實施規範沒有因最新修正公告而誤刪
- `areas` 查詢正確，多業態資料可由任一業態查到
- 官方來源仍可追溯
- GitHub Pages 使用 `main /docs`

## 12. 對 ChatGPT 的指令

當使用者只說：

「更新 food-reg-dashboard，照現有架構處理」

應視為以下完整指令：

> 先讀取 repository 根目錄 `AGENTS.md`、`README.md`、`docs/data/manifest.json` 與相關模組 JSON；依本檔案規範先在 `dev` 修改，使用官方來源查證，保留完整現行規定、歷史沿革及未來已公告規範，確認無誤後再同步到 `main`，不得把法規資料重新塞回 `index.html`。
